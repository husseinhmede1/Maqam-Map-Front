# Base URL of the MaqaMap API.
# Leave empty in development to use the Vite proxy (/api -> localhost:4000).
VITE_API_BASE_URL=

# Target the dev proxy forwards to
VITE_DEV_API_TARGET=http://localhost:4000
